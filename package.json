{
  "name": "elder-android-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone backend for the Coastal Elder Scheduler Android app. Shares only the Airtable base with coastal-elder-scheduler — no other code, logic, or infra is shared between the two.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  }
}
